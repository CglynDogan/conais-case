{
  "name": "interview-assistant",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Real-time sales call coaching assistant",
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev"
  }
}
